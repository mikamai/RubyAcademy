#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'todo_list'

list = TodoList.new

if ARGV.first == '--list'
  puts list.to_s(:done)
else
  list.do!
  puts list
end
