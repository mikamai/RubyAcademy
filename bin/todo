#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'todo_list'

list = TodoList.new

unless ARGV.empty?
  list << ARGV.join(' ')
  list.persist!
end

puts list.to_s(:todo)

