#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'todo_list'

list = TodoList.new('~/.ra-todos')

unless ARGV.empty?
  list << ARGV.join(' ')
  list.persist!
end

puts list

